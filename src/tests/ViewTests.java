package tests;

import Controller.Main;
import Model.Command;
import Model.Instruction;
import View.Primary;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ViewTests {

    @Test
    void registerUpdate() {


        // Check ob Inhalt in (UI) Register sich aktualisiert

        // Noetige Objekte erstellen

        Primary view = new Primary();
        Main manager = new Main();
        


        // Inhalt von Register abspeichern


        String before;

        assertEquals(0, 1);
    }
}